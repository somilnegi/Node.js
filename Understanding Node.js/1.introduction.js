// node.js is not a framework or a library. It is a runtime environment that allows you to run JavaScript code outside of a web browser. It is built on the V8 JavaScript engine, which is the same engine that powers Google Chrome. Node.js is designed to be lightweight and efficient, making it ideal for building scalable network applications.

console.log("Hello World");

// To run this code, use command: node firstcode.js
// The command node --watch firstcode.js can be used to automatically rerun the code whenever the file is saved.

// JavaScript has 2 environments:
// 1. Browser Environment
// 2. Node.js Environment
// The main difference between the two is that the browser environment has access to the Document Object Model (DOM) and other web APIs, while the Node.js environment has access to the file system, network, and other server-side APIs.